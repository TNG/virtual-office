{
  "name": "Virtual Office GraphQL Remote Server Schema",
  "schemaPath": "server/apollo/schema.ts",
  "excludes": ["server/build/**"],
  "extensions": {
    "endpoints": {
      "Default GraphQL Endpoint": {
        "url": "http://localhost:9000/graphql",
        "headers": {
          "user-agent": "JS GraphQL"
        },
        "introspect": false
      }
    }
  }
}
